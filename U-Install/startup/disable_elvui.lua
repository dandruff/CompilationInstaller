-- Disable ElvUI Installer
local _, E, L, C = nil, unpack(select(2, ...))

-- Uncomment the line below to disable the ElvUI Installer
-- if ElvUI then E.ElvUIInstallCache = ElvUI[1].Install; ElvUI[1].Install = E.noop end
